function FooterActions (props) {
  return (
    <footer className="footer-actions">
      <div className="inner">
        {props.children}
      </div>
    </footer>
  )
}

export default FooterActions
